node_modules/
.next/
.env*.local
*.db
*.db-wal
*.db-shm
backups/
*.tsbuildinfo

# Local DB backups (prod-data snapshots taken during dev — never commit)
.local-db-backup/

# Playwright e2e artifacts (generated per run)
/test-results/
/playwright-report/
/tests/e2e/.auth-state.json
/tests/e2e/.backups/

# Bot runtime artifacts (live on the server, never committed)
/bot/bot.log
/bot/media/

# Packaged skill bundles (build artifacts)
skills/*.zip
