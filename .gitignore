node_modules/
.next/
.env*.local
CLAUDE.local.md
AGENTS.local.md
.private/
docs/private/
*.db
*.db-wal
*.db-shm
backups/
*.tsbuildinfo

# Local DB backups (prod-data snapshots taken during dev — never commit)
.local-db-backup/

# Recallatron entity graph — server-only live data, same class as mot.db (OQ-1=B)
/ontology/

# Track 5 fastembed model download cache — server-only live data, same class as mot.db
# (~90MB, deploy-preserved). Never committed.
.model-cache/

# Playwright e2e artifacts (generated per run)
/test-results/
/playwright-report/
/tests/e2e/.auth-state.json
/tests/e2e/.backups/

# Bot runtime artifacts (live on the server, never committed)
/bot/bot.log
/bot/media/

# Packaged skill bundles (build artifacts)
skills/*.zip
.bureau/runs/
.bureau/archive/
